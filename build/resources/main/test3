% Programa de Teste
Calculo de idade%
INIT
    cont is int;
    media, idade, soma is integer;
   begin
      cont := 5;
      soma := 0;
do
      while(cont > 0)
      media := soma / qts
      write("Media: ");
      write (media);
STOP